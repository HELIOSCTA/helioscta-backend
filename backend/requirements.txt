# db
psycopg2-binary==2.9.10
pyodbc

# ipython / dev
ipython
jupyter
notebook

# python
python-dotenv[cli]==1.0.1
numpy
pandas>=2.0.0
pyarrow>=12.0.0
tabulate
openpyxl>=3.1.0

# gridstatus
gridstatusio
gridstatus==0.34.0

# sftp
paramiko

# orchestration
tenacity>=8.2

# http
requests>=2.31

# azure
azure-storage-blob>=12.19.0
azure-core>=1.29.0
fsspec>=2024.1.0
adlfs>=2024.1.0

# slack
slack-sdk

# mcp_server (backend-views)
fastapi>=0.110
fastapi-mcp>=0.4
uvicorn[standard]>=0.27

# ──── transformations (local-only — excluded from Dockerfile.prefect) ────
# Everything below this marker is filtered out of the Prefect worker
# image. dbt powers backend/dbt/ during local dev (compile-only against
# a read-only Postgres role) and does not need to ship in the scrapes
# container.
dbt-core>=1.8
dbt-postgres>=1.8
