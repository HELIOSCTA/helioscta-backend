Improve this prompt for claude code:

I want to now manage a data catalogue from a vendor's database. There is no formal documentation from the vendor but we have full access to their databases.

I want my proxy dbt model (dbt_criterion) to be a data catalogue.md and a list of all the sql files.

The old code sits in .refactor\dbt_criterion_postgresql

The excel models sit in TODO\dbt_criterion_postgresql\excel_files

I can add mcp connections to build this out with creditenials in the .env file