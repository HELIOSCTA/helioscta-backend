# helioscta-backend

Monorepo for HeliosCTA's backend (`backend/`, Python scrapes + shared
utilities) and frontend (`frontend/`, Next.js 15 dashboard). See
[`CLAUDE.md`](./CLAUDE.md) for the top-level map.
